{
  "rules": {
    ".read": true,
    ".write": "auth !== null",
    "users": {
      ".indexOn": ["username", "email"],
      "username": {
        ".write": "!data.exists() || !newData.exists()"
      }
    }
  }
}
